# チャット実装メモ

## 実装日

- 2026-03-27

## 実装内容

- `assistant-ui` を導入
- `src/app/api/chat/route.ts` を追加して OpenAI 接続口を用意
- 左カラムのダミーチャットを実チャット UI に差し替え
- チャットのシステムメッセージを予定調整向けに調整
- `.env.example` を追加して必要な環境変数を明示

## 現在の構成

- UI: `assistant-ui`
- API route: `src/app/api/chat/route.ts`
- モデル指定: `OPENAI_MODEL`
- API キー: `OPENAI_API_KEY`

## 現時点の制約

- チャット内容はまだタイムラインへ自動反映しない
- 認証やユーザー単位保存とは未接続
- 添付や高度なアクションは初期版では省略している

## 次に必要な実装

- チャット結果から予定候補を構造化データとして返す
- タイムライン側に候補を仮配置する
- Supabase Auth とユーザーデータ保存を接続する
