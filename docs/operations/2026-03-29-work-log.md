# 2026-03-29 作業ログ

## 実施内容
- 公開トップ `/` を導入した。
- Google ログイン用ページ `/login` を追加した。
- ログイン後ホームを `/app` に分離した。
- 既存アプリページにログイン必須のガードを追加した。
- `.env.example` を開発者向けの説明に整理した。
- Supabase 初期スキーマを一括 SQL として追加した。
- チャット画面を ChatGPT 寄りの単一カラム構成に寄せ、入力欄左の `+` から添付できるようにした。
- チャット API から Google Calendar の読み書き、Google Sheets 読み取り、Google Maps 経路計算を呼べるようにした。

## 現在の状態
- 未ログイン時は公開トップとログインページだけを使う。
- ログイン後は既存のアプリ画面に入れる。
- Supabase Auth と Google OAuth の土台は入っている。
- DB は `supabase/migrations/202603290001_initial_app_schema.sql` で一括作成できる。
- `Import entry` と `Message examples` はチャット画面から外し、添付導線を Composer に統合した。
- AI は会話の中で既存予定確認、シート確認、移動時間確認、カレンダー登録まで進められる。
- チャット添付画像は最新メッセージから抽出し、会話文脈に予定候補として渡すようにした。
- 添付だけ送った場合でも本文を補って AI が応答しやすいように調整し、API 側で添付受信ログを確認できるようにした。
- 画像・シート抽出プロンプトを日本語で再構築し、構造化結果が空のときは明示的にエラーを返すようにした。

## 次の候補
- Supabase SQL Editor へ流し込み
- events / profiles を Supabase から読むように切り替え
- Google Calendar / Sheets のユーザー単位動作確認
- Places API 導入
