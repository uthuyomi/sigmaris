# 2026-03-27 作業ログ

## 方針メモ
- 会話で決まった内容は `docs/decisions/` に記録する。
- 実装の流れや進捗は `docs/operations/` に積む。
- README と設計メモは Notion に移しやすい章立てで保つ。

## 実施済み
- `Next.js + TypeScript` のアプリ基盤を用意した。
- `Supabase` をユーザーデータ保存基盤として採用する方針を決めた。
- 初期認証は `Google OAuth` 一本で進める方針に整理した。
- チャット主体 UI と時間ベースのタイムライン UI を実装した。
- カレンダーで日付を選んでからタイムラインへ降りる導線にした。
- Google Sheets URL と画像ファイルをチャット導線で取り込む土台を入れた。
- Google Calendar への書き込み基盤を追加した。
- Google Maps を使った移動計画パネルをタイムラインへ追加した。
- Supabase SSR と Google OAuth コールバックの土台を追加した。
- 公開トップ `/`、ログイン `/login`、アプリ側 `/app` の導線に分離した。

## 今の状態
- チャット、カレンダー、タイムライン、設定の主要画面はそろっている。
- 取り込み候補のプレビューと Google Calendar 反映ができる。
- 場所付き予定に対して、推奨出発時刻と経路概要を出せる。
- Google ログイン UI とセッション保持の配線が入っている。
- 未ログイン時は既存アプリ画面から `/login` へ誘導する。

## 次の作業候補
- Supabase 側の実プロジェクト接続確認
- Google OAuth の本番用設定
- Places API を使った場所補完
- チャットからの「反映して」で予定登録まで完結させる配線
