# 2026-03-27 作業ログ

## 実施内容

- ドキュメント構成を日本語化し、Notion へ移しやすい形に整理
- 初期技術方針として `Next.js + TypeScript + Supabase` を採用
- 認証基盤として `Supabase Auth` を採用
- 初期認証プロバイダとして `Google` `GitHub` `Discord` を選定
- チャット中心 UI とモバイル寄せデザインの技術方針を整理
- 予定管理を 24 時間の時間帯ベースで扱う方針を確定
- 予定編集の時間粒度は UI 切り替え式とし、デフォルト 10 分刻み / 最小 5 分刻みの方針を決定
- 粒度切り替え候補を `5 / 10 / 15 / 30 / 60 分` に確定
- 標準的なデイタイムライン UI をベースにした操作仕様を定義
- ワイヤーフレームは省略し、UI 実装先行で進める方針を決定
- Next.js アプリ基盤を作成し、チャット画面と 24 時間タイムラインの初期 UI を実装
- 初期 UI について `npm run lint` と `npm run build` を通過

## 判断メモ

- ユーザーごとのデータ保存を前提に、早い段階で Supabase を導入する方針にした
- 認証は独自実装ではなく Supabase Auth を利用する
- 認証手段は初期実装を絞りつつ、後から拡張しやすい構成を優先した

## 次にやること

- 認証方式を前提にした最小データモデルを定義する
- Next.js アプリの初期セットアップ方針を固める
- Supabase の環境変数と接続方針を設計する
- 予定データの最小モデルを開始日時 / 終了日時ベースで定義する
- タイムライン UI のコンポーネント分解を行う
- Supabase Auth と UI を接続する
- タイムラインのドラッグ操作と編集動作を実装する
